events {
    worker_connections 1024;
}

http {
    # upstream aitoys_backend {
    #     server aitoys-server:8000;
    # }
    
    # upstream aitoys_admin {
    #     server aitoys-admin-server:8100;
    # }
    
    # upstream aitoys_ota {
    #     server aitoys-ota-server:8020;
    # }

    # upstream aitoys_manager {
    #     server aitoys-manager-server:8010;
    # }

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log;

    # 基本设置
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # HTTP服务器
    server {
        listen 80;
        server_name _;

        # 客户端最大请求体大小
        client_max_body_size 100M;

        # # WebSocket代理
        # location /aitoys/v1/ {
        #     proxy_pass http://aitoys_backend;
        #     proxy_http_version 1.1;
        #     proxy_set_header Upgrade $http_upgrade;
        #     proxy_set_header Connection "upgrade";
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
            
        #     # WebSocket超时设置
        #     proxy_read_timeout 86400;
        #     proxy_send_timeout 86400;
        #     proxy_connect_timeout 60;
            
        #     # 缓冲设置
        #     proxy_buffering off;
        #     proxy_cache off;
        # }

        # # 后台管理系统代理
        # location /admin/ {
        #     proxy_pass http://aitoys_admin/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        # # OTA服务代理
        # location /ota/ {
        #     proxy_pass http://aitoys_ota/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }
        
        # # MANAGER服务代理
        # location /manager/ {
        #     proxy_pass http://aitoys_manager/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        # # 健康检查
        # location /health {
        #     proxy_pass http://aitoys_backend/aitoys/v1/health;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        # 静态文件（如果有）
        location /static/ {
            alias /var/www/static/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # 文件下载服务
        location /downloads/ {
            alias /app/docker/runtime/downloads/;
            expires 1d;
            add_header Cache-Control "public";
            add_header Content-Disposition "attachment";
            
            # 支持APK文件下载
            location ~* \.(apk)$ {
                add_header Content-Type "application/vnd.android.package-archive";
            }
        }

        # 默认错误页面
        error_page 404 /404.html;
        error_page 500 502 503 504 /50x.html;
        
        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }

} 